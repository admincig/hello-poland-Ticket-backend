stages:
    - build

variables:
    JAVA_VERSION: "10"

#
# build stage tasks
#
build_backend:
    stage: build
    before_script:
        - . envcfg
        - envcfg -p
    script:
        - mvn -U clean verify
